const useTraerseTree = () => {
  function insertNode(tree, folderId, item, isFolder) {
    if (tree.id === folderId && tree.isFolder) {
    }
  }
  return { insertNode };
};

export default useTraerseTree;
